import { SqlConverter } from "@/components/SqlConverter";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background via-muted/30 to-background py-12 px-4 sm:px-6 lg:px-8">
      <SqlConverter />
    </div>
  );
};

export default Index;
